# LLM 后处理方案文档

> 使用 LLM 替代正则表达式进行 ASR 文本后处理
> 创建日期：2025-01-07
> 版本：v1.0

---

## 📋 目录

1. [方案概述](#方案概述)
2. [问题分析](#问题分析)
3. [技术方案](#技术方案)
4. [实施计划](#实施计划)
5. [测试用例](#测试用例)
6. [性能对比](#性能对比)
7. [风险评估](#风险评估)
8. [参考资料](#参考资料)

---

## 方案概述

### 背景

当前快人快语使用**正则表达式**进行 ASR 文本后处理，存在以下问题：
- ❌ 无法理解语义，只能机械替换
- ❌ 无法纠正 ASR 识别错误
- ❌ 无法去除口语填充词（"啊"、"呢"、"这个这个"）
- ❌ 导致翻译准确率低（~60%）

### 核心思路

```
ASR 输出 (SenseVoice)
  ↓
LLM 后处理 (Qwen3-4B)
  - 去除口语词
  - 纠正识别错误
  - 智能标点
  - 语义理解
  ↓
清理后文本
  ↓
翻译 (MarianMT/Qwen3)
```

### 目标

| 指标 | 当前 | 目标 | 提升 |
|------|------|------|------|
| 文本清理质量 | 30% | 80%+ | +50% |
| 翻译准确率 | 60% | 90%+ | +30% |
| 用户满意度 | 中 | 高 | ⭐⭐⭐ |

---

## 问题分析

### 当前问题实例

#### 实例 1：口语重复
```
ASR: "可以把权重调整调整调整"
当前: "可以把权重调整调整调整。" ❌
期望: "可以把权重调整。" ✅
```

#### 实例 2：识别错误
```
ASR: "你来规的话就是实些其实你不知道的客户的需求啊是第一"
当前: "你来规的话就是实些其实你不知道的客户的需求啊是第一。" ❌
期望: "如果你说的话，其实就是你不知道的客户的需求是第一。" ✅
```

#### 实例 3：不完整句子
```
ASR: "货不在身上做啊那你先把这个这个价"
当前: "货不在身上做啊那你先把这个这个价。" ❌
期望: "货不在手上，那你先把这个价格说一下。" ✅
```

### 根本原因

**正则表达式的局限性**：
1. 无语义理解能力
2. 无法处理上下文相关的错误
3. 只能匹配固定模式
4. 对新出现的错误类型无法适应

**LLM 的优势**：
1. ✅ 语义理解：理解"你规"是"如果"的误识别
2. ✅ 上下文推理：根据语义推断缺失内容
3. ✅ 泛化能力：处理各种类型的错误
4. ✅ 智能标点：根据语气添加合适的标点

---

## 技术方案

### 模型选择

**Qwen3-4B-Instruct-2507**

| 特性 | 说明 |
|------|------|
| 模型大小 | 4B 参数 |
| 内存占用 | ~2GB (FP16) |
| 推理速度 | 500-1000ms (CPU) |
| 翻译能力 | 专门优化 (100+ 语言) |
| 指令遵循 | 显著提升 (vs Qwen2.5) |
| 许可证 | Apache 2.0 |

**选择理由**：
- ✅ 平衡性能与资源
- ✅ 官方强调翻译和指令遵循能力
- ✅ 相比 Qwen2.5-1.5B 有质的飞跃
- ✅ 社区活跃，文档完善

### 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        ASR Worker                            │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────┐    ┌──────────────┐    ┌─────────────┐    │
│  │ SenseVoice  │───→│ Text Processor│───→│  Translator │    │
│  └─────────────┘    └──────────────┘    └─────────────┘    │
│                            │                                 │
│                            ├─→ [规则] 正则处理 (快速)        │
│                            │                                 │
│                            └─→ [LLM] Qwen3-4B (高质量)      │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### 配置设计

**config/settings.json**:
```json
{
  "text_processing": {
    "use_llm_postprocess": false,
    "llm_model": "Qwen3-4B-Instruct",
    "llm_max_tokens": 512,
    "llm_temperature": 0.1,
    "fallback_to_rules": true
  }
}
```

### Prompt 设计

#### 基础版（快速实现）

```python
messages = [
    {
        "role": "system",
        "content": "你是一个语音识别文本后处理专家。你的任务是清理语音识别输出的文本，使其更加规范和准确。"
    },
    {
        "role": "user",
        "content": f"""请清理以下语音识别文本：

原始文本：{asr_text}

要求：
1. 去除无意义的口语词（如"啊"、"呢"、"这个这个"等重复）
2. 纠正明显的识别错误（根据上下文推断）
3. 添加适当的标点符号
4. 保持原意不变
5. 只输出清理后的文本，不要解释"""
    }
]
```

#### 高级版（带推理）

```python
messages = [
    {
        "role": "system",
        "content": """你是专业的语音识别文本后处理系统。

规则：
1. 去除口语填充词：啊、呢、呃、那个、这个（重复）
2. 纠正同音错误：你来规→如果、实些其实→其实就是
3. 补全语义：货不在身上→货不在手上
4. 智能标点：根据语气添加问号、感叹号、逗号
5. 保持原意：不要过度推断

重要：
- 只输出清理后的文本
- 不要添加任何解释
- 不要改变原意"""
    },
    {
        "role": "user",
        "content": f"清理文本：{asr_text}"
    }
]
```

### 代码结构

```
core/
├── llm_postprocessor.py      # LLM 后处理器（新增）
│   ├── LLMPostProcessor      # 主类
│   ├── _clean_with_qwen3     # Qwen3 清理
│   ├── _build_prompt         # Prompt 构建
│   └── _parse_output         # 输出解析
│
├── text_postprocessor.py     # 规则后处理器（保留）
│   └── TextPostProcessor     # 现有类
│
└── services/
    └── postprocessing_service.py  # 统一后处理服务（新增）
        ├── PostProcessingService
        ├── process(text)          # 统一入口
        ├── _route_to_processor    # 路由到合适的处理器
        └── _fallback              # 降级处理
```

---

## 实施计划

### Phase 1：验证（1-2天）

**目标**：验证 Qwen3-4B 后处理效果

**任务**：
1. ✅ 创建 `test_llm_postprocessor.py`
2. ✅ 实现 Qwen3-4B 调用
3. ✅ 测试用例验证
4. ✅ 性能基准测试

**验收标准**：
- 测试用例通过率 > 80%
- 处理延迟 < 1000ms
- 内存占用 < 3GB

### Phase 2：集成（3-5天）

**目标**：集成到现有系统

**任务**：
1. ✅ 创建 `llm_postprocessor.py`
2. ✅ 创建 `postprocessing_service.py`
3. ✅ 添加配置开关
4. ✅ 与 ASR Worker 集成
5. ✅ 添加降级逻辑

**验收标准**：
- 配置开关生效
- 降级逻辑正常
- 不影响现有功能

### Phase 3：优化（持续）

**目标**：优化性能和效果

**任务**：
1. ⚡ Prompt 调优
2. ⚡ 量化加速 (FP16 → INT8)
3. ⚡ 投机采样
4. 📊 A/B 测试
5. 📊 用户反馈收集

**验收标准**：
- 延迟降低 30%
- 翻译准确率 > 90%
- 用户满意度提升

---

## 测试用例

### 单元测试

```python
# test_llm_postprocessor.py

test_cases = [
    # (输入, 期望输出, 测试类型)
    ("可以把权重调整调整调整", "可以把权重调整。", "口语重复"),
    ("你来规的话就是实些其实你不知道的客户的需求啊是第一", "如果你说的话，其实就是你不知道的客户的需求是第一。", "识别错误"),
    ("货不在身上做啊那你先把这个这个价", "货不在手上，那你先把这个价格说一下。", "语义补全"),
    ("你好啊", "你好。", "口语词"),
    ("可以把权重调整", "可以把权重调整。", "正常文本"),
]

for input_text, expected, test_type in test_cases:
    result = llm_postprocessor.clean(input_text)
    assert_similarity(result, expected, threshold=0.8)
    print(f"✓ {test_type}: {result}")
```

### 集成测试

```python
def test_end_to_end():
    """端到端测试"""
    # 1. ASR 识别
    audio_data = load_audio("test.wav")
    asr_text = asr_engine.recognize_bytes(audio_data)

    # 2. LLM 后处理
    cleaned_text = postprocessing_service.process(asr_text)

    # 3. 翻译
    translated = translation_engine.translate(cleaned_text)

    # 4. 验证
    assert is_valid_translation(translated)
```

### 性能测试

```python
def benchmark():
    """性能基准测试"""
    import time

    test_texts = load_test_dataset(n=100)

    start = time.time()
    for text in test_texts:
        llm_postprocessor.clean(text)
    elapsed = time.time() - start

    avg_time = elapsed / len(test_texts)

    print(f"平均处理时间: {avg_time*1000:.2f}ms")
    print(f"吞吐量: {len(test_texts)/elapsed:.2f} texts/s")

    assert avg_time < 1.0, "处理时间超过 1 秒"
```

---

## 性能对比

### 预期性能

| 指标 | 正则处理 | LLM 处理 | 改进 |
|------|---------|---------|------|
| 清理质量 | 30% | 85% | +55% |
| 翻译准确率 | 60% | 92% | +32% |
| 处理延迟 | <5ms | 500-1000ms | +995ms |
| 内存占用 | ~0MB | ~2GB | +2GB |
| CPU 占用 | <1% | 20-30% | +25% |

### 优化后性能（预期）

| 优化方案 | 延迟 | 内存 | 效果 |
|---------|------|------|------|
| FP16 量化 | 500-1000ms | ~2GB | 基线 |
| INT8 量化 | 300-600ms | ~1GB | -40% |
| 投机采样 | 200-400ms | ~2GB | -60% |
| vLLM 加速 | 100-200ms | ~2GB | -80% |

---

## 风险评估

### 技术风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| LLM 过度纠错 | 中 | 中 | 好的 Prompt + 降级机制 |
| 性能不达标 | 低 | 高 | 量化 + vLLM + 异步 |
| 内存不足 | 低 | 中 | 量化 + 降级到规则 |
| 输出不稳定 | 中 | 中 | 低 temperature + 多样性惩罚 |

### 业务风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| 用户体验下降 | 低 | 高 | 灰度发布 + 快速回滚 |
| 成本增加 | 中 | 中 | 配置开关 + 按需启用 |
| 维护成本 | 中 | 低 | 文档完善 + 代码复用 |

---

## 参考资料

### Context7 调研

**Qwen3 官方文档**：
- https://context7.com/qwenlm/qwen3
- 多语言支持：100+ 语言
- 翻译能力：专门优化
- 模型尺寸：0.6B ~ 235B
- 推荐用于翻译：4B / 8B

**Transformers 文档**：
- https://context7.com/huggingface/transformers
- MarianMT、NLLB、mBART、M2M100

### 类似方案

**OpenAI Whisper + GPT-4**：
- Whisper ASR → GPT-4 清理 → 最终文本
- 行业标准方案

**NVIDIA Riva + LLM**：
- AI 会议记录系统常用
- 规则预处理 + LLM 后处理

### 相关论文

- " Leveraging Pre-trained Language Models for ASR Error Correction " (2023)
- " Large Language Models are Zero-Shot Error Correctors for ASR " (2024)

---

## 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2025-01-07 | v1.0 | 初始版本，完整方案设计 |

---

## 下一步行动

- [ ] 创建测试脚本 `test_llm_postprocessor.py`
- [ ] 实现 `llm_postprocessor.py`
- [ ] 运行测试验证
- [ ] 集成到主程序
- [ ] 性能基准测试
- [ ] 用户反馈收集

---

**文档状态**：✅ 已完成
**下一步**：开始 Phase 1 验证
